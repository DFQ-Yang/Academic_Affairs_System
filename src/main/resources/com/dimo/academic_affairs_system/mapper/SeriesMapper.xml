<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dimo.academic_affairs_system.mapper.SeriesMapper">
    <delete id="delete_series">
        delete from relation.etc where course_id in (select id from class.course where series_id = #{seriesId});
        delete from relation.stc where course_id in (select id from class.course where series_id = #{seriesId});
        delete from class.series where id = #{seriesId};
        delete from class.course where series_id = #{seriesId};
    </delete>
</mapper>