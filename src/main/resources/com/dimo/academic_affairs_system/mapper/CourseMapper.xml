<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dimo.academic_affairs_system.mapper.CourseMapper">
    <delete id="delete_course">
        delete from relation.stc where course_id = #{courseId};
        delete from relation.etc where course_id = #{courseId};
        delete from class.course where id = #{course_id}
    </delete>
</mapper>
